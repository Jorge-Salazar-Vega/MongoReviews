{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && dotnet publish -c Release -o /app"
  },
  "deploy": {
    "command": "dotnet /app/MongoReviews.Api.dll",
    "healthcheckPath": "/api/series",
    "restartPolicyType": "ON_FAILURE"
  }
}
